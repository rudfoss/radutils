import { CacheConfigSource } from "./CacheConfigSource"

describe("CacheConfigSource", () => {
	it("is defined", () => {
		expect(CacheConfigSource).toBeDefined()
	})

	it("does not return anything on first run", async () => {

	})
	it("returns cached values on multiple successfull runs", async() => {

	})

	it("does not cache if build fails", () => {
		
	})
})
