Utils:
	Config Builder:
		☐ Core builder class
		☐ Loader support
		☐ Documentation
		☐ Examples
	Config Loader Azure:
		Azure App Configuration and/or Azure Key Vault
		☐ App Configuration support
		☐ Content type resolver support
		☐ Key Vault reference support
		☐ Documentation
	Config Loader ENV:
		Load from ENV variables
		☐ ENV Loader
		☐ Documentation
	Config Loader JSON:
		Load from JSON file or static JSON object
		☐ JSON Loader
		☐ Documentation
	Config Loader .rc:
		Load from .rc and .rc compatible sources (low-priority)
		☐ .rc Loader
		☐ Documentation
	Config Loader Cached:
		Cache config data locally as a file in `node_modules/.cache` for quick restarts.
		☐ Cached data loader (returns cached version of the config data from disk after initial load)
		☐ Documentation
	Query manager (with caching, like DataQuery) *
	Observable Map *
